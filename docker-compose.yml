services:
  server:
    build: .
    ports:
      - 25565:25565/tcp
      - 19132:19132/udp
    restart: unless-stopped

    # The following allow `docker attach` to work
    stdin_open: true
    tty: true
